# old 백업 안내

요청에 따라 기존 구현은 `old` 분기로 관리할 수 있도록 분리 메모를 남깁니다.

- 현재 적용된 변경:
  - 타워 뽑기 풀을 `1~151` 기준으로 변경
  - 진화 가능한 라인은 1단만 뽑기
  - 전설 ID 구분(`144,145,146,150,151`) 추가
- 이전 방식(전세대 라인 기반 뽑기, 역할/배치 실험)은 Git 이력으로 복구 가능합니다.

필요하면 다음 단계에서 `gameDemo.js`와 `pokemonConfig.js`의 구버전 스냅샷 파일을 `js/old/`로 별도 분기 저장하겠습니다.
